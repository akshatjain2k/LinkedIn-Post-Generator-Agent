# LangChain / LangGraph — agent framework
langchain>=1.3.0
langgraph>=1.2.0
langchain-openai>=1.3.0
langchain-community>=0.4.0
langchain-nvidia-ai-endpoints>=1.4.0

# OpenAI-compatible HTTP client (used by langchain-openai)
openai>=2.44.0

# Pydantic — SecretStr for safe API key handling
pydantic>=2.0.0

# Web search
google-search-results>=2.4.2

# PDF generation — works on Windows/Mac/Linux, no system fonts needed
reportlab>=5.0.0

# MCP server
mcp[cli]>=1.28.0

# Environment / config
python-dotenv>=1.0.0
